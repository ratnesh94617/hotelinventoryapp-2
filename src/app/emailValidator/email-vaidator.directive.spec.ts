import { EmailVaidatorDirective } from './email-vaidator.directive';

describe('EmailVaidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailVaidatorDirective();
    expect(directive).toBeTruthy();
  });
});
